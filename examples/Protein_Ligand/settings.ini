radType = 1         # radius type of atoms (0:ff, 1:mBondi), default: mBondi
radDef = 1.5        # default atom radius for LJ parameters (A), if radius not available
cfac = 3            # Factor to expand mol-dim to get coarse grid dim
fadd = 10           # Amount added to mol-dim to get fine grid dim (A)
r_cutoff = 0        # atom distance cutoff, 0: infinity
df = 0.5            # The desired fine mesh spacing (A)

nkernels = 8        # Number of physical cores, not logical cores
preserve = "y"      # whether to preserve intermediate files

gmx = "gmx"         # Path of Gromacs, if "built-in", then programs/gmx/win/gmx.exe will be used
apbs = "apbs"       # Path of APBS, leave empty if do not want to do PBSA calculations

last_opened = "examples\Protein_Ligand\md.tpr"
